name: Main Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  call-reusable:
    uses: ./java.tech/.github/workflows/template.yml@main
    with:
      java-version: '17'
      maven-goals: 'clean install'
    secrets:
      MAVEN_SECRET: ${{ secrets.MAVEN_SECRET }}

